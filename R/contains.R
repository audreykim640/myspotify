# list --> original thing we want to check against
# checkfor --> see if these items exist in list

# contains <- function(list, checkfor = c("")) {
#   df_names <- colnames(df)
#   for userinput in cols {
#     if
#   }
# }
